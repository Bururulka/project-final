spring.cache.type: none
spring:
  datasource:
    url: jdbc:h2:mem:jira-test;MODE=PostgreSQL
    username: sa
    password: password
    driver-class-name: org.h2.Driver
  sql:
    init:
      mode: always
  liquibase:
    change-log: "classpath:changelog.sql"
